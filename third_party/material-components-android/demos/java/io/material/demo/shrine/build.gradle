apply plugin: 'com.android.application'

dependencies {
  compile project(':demos:java:io:material:demo:shrine:products')
}

android {
  sourceSets {
    main {
      java.includes = ['*.java']
      java.srcDirs = ['.']
      res.srcDirs = ['res']
      manifest.srcFile 'AndroidManifest.xml'
    }
  }
}
