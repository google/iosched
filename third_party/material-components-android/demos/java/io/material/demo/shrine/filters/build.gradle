apply plugin: 'com.android.library'

dependencies {
  compile project(':demos:java:io:material:demo:shrine:theme')
}

android {
  sourceSets {
    main {
      java.includes = ['*.java']
      java.srcDirs = ['.']
      res.srcDirs = ['res']
      manifest.srcFile 'AndroidManifest.xml'
    }
  }
}
